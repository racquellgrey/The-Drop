{
  "name": "the-drop",
  "private": true,
  "version": "1.0.0",
  "description": "The Drop — limited-edition sneaker drop platform (Vercel deploy)",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix backend",
    "build": "npm install --prefix backend",
    "dev": "npm run dev --prefix backend",
    "start": "npm start --prefix backend",
    "lint:syntax": "npm run lint:syntax --prefix backend",
    "audit": "npm run audit --prefix backend"
  }
}
